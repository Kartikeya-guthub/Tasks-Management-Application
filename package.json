{
  "name": "task-management-application",
  "version": "1.0.0",
  "description": "Full-stack Task Management Application — Express backend + React/Vite frontend",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "start": "npm run start --workspace=backend",
    "migrate": "npm run migrate --workspace=backend",
    "test": "concurrently \"npm run test --workspace=backend\" \"npm run test --workspace=frontend\"",
    "lint": "concurrently \"npm run lint --workspace=backend\" \"npm run lint --workspace=frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
